# name: Deploy Tauri LifeOS App Dev

# on:
#   push:
#     branches:
#       - dev
#     paths:
#       - "apps/lifeos/taurireact-macapp/**"
#       - "packages/holaaiconvex/**"
#       - ".github/workflows/deploy-tauri-lifeosapp-dev.yml"

# env:
#   VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
#   VERCEL_PROJECT_ID: ${{ secrets.VERCEL_TAURI_LIFEOSAPP_PROJECT_ID }}

# jobs:
#   deploy-dev:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"

#       - name: Setup pnpm
#         uses: pnpm/action-setup@v4
#         with:
#           version: 10.20.0

#       - name: Install dependencies
#         run: pnpm install

#       # Note: Convex dev deployment is for local development only
#       # CI deploys to Convex on staging (adorable-firefly-704) and prod (agreeable-ibex-949) branches

#       - name: Install Vercel CLI
#         run: npm install -g vercel

#       - name: Pull Vercel Environment
#         run: vercel pull --yes --environment=preview --token=${{ secrets.VERCEL_TOKEN }}

#       - name: Build Project
#         run: vercel build --token=${{ secrets.VERCEL_TOKEN }}

#       - name: Deploy to Vercel
#         run: vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }}
