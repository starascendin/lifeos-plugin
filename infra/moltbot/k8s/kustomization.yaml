apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: moltbot

resources:
  - namespace.yaml
  - deployment.yaml
  - secrets.yaml

commonLabels:
  app.kubernetes.io/name: moltbot
  app.kubernetes.io/part-of: ai-experiments

images:
  - name: ghcr.io/starascendin/moltbot
    newTag: latest
