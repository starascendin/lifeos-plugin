---
# Convex credentials for controlplane
# NOTE: In production, this should be a SealedSecret
apiVersion: v1
kind: Secret
metadata:
  name: convex-credentials
  namespace: controlplane
type: Opaque
stringData:
  CONTROLPLANE_API_KEY: "controlplane-api-key-2024"
