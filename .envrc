# Devcontainer detection
if [ -d ".devcontainer" ] && [ -z "$DEVCONTAINER" ]; then
  echo ""
  echo "📦 Devcontainer available:"
  echo "   dcenter  - Enter devcontainer shell"
  echo "   dcyolo   - Enter with Claude in yolo mode"
  echo ""
fi

# Load .env files if they exist (for local dev)
dotenv_if_exists .env
dotenv_if_exists .env.local
