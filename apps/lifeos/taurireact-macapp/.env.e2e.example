# Playwright E2E (webapp mode)
E2E_BASE_URL=http://localhost:1420

# -----------------------------
# Convex (choose one approach)
# -----------------------------
#
# Option A: Run against a Convex preview deployment created per test run
# (recommended for E2E isolation)
#
# Your preview deploy key format:
#   preview:<team>:<project>|<token>
CONVEX_DEPLOY_KEY=preview:starascendin:holaaiconvex-staging|your_preview_deploy_key
# Optional: override the generated preview name
E2E_CONVEX_PREVIEW_NAME=
#
# Option B: Run against an existing deployment by setting VITE_CONVEX_URL below.

# -----------------------------
# Clerk (required)
# -----------------------------
#
# Get from Clerk Dashboard > API Keys > Testing Token
CLERK_TESTING_TOKEN=your_clerk_testing_token
#
# Required by @clerk/testing (same value as VITE_CLERK_PUBLISHABLE_KEY)
CLERK_PUBLISHABLE_KEY=pk_test_...
# Some @clerk/testing helpers require a secret key (from Clerk Dashboard)
CLERK_SECRET_KEY=sk_test_...

# -----------------------------
# Test user (required)
# -----------------------------
# Create a user in Clerk matching these (Email/Password auth enabled)
E2E_TEST_USER_EMAIL=e2e-test@example.com
E2E_TEST_USER_PASSWORD=your_test_password
E2E_TEST_USER_2FA_CODE=424242

# -----------------------------
# App runtime (Vite)
# -----------------------------
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# If using `pnpm test:e2e:preview`, this will be overridden automatically.
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Dev/Staging-only convenience UI (do not enable in production)
VITE_ENABLE_TEST_USER_LOGIN=false
