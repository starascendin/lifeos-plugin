[livekit]
# LiveKit project configuration
# Deploy with: lk app deploy

[app]
name = "holaai-spanish-tutor"
main = "main.py"
runtime = "python"

[env]
# These will be set during deployment or via .env.local
# LIVEKIT_URL = "wss://<project>.livekit.cloud"
# LIVEKIT_API_KEY = "<key>"
# LIVEKIT_API_SECRET = "<secret>"
# GOOGLE_API_KEY = "<gemini-key>"
