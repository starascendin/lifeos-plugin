# Super Simple Spec – Final Version  
“Substack Notes Scheduler” Chrome Extension  
→ Everything happens in a **new tab** (not a tiny popup)

### Why a new tab is better for this use case
- Full-screen rich editor (exactly like Substack’s own composer)  
- Easy drag & drop images, formatting, preview  
- Calendar looks nice and big  
- Can keep the tab open all day while batch-writing  
- Still 100 % offline, no backend, no API

### Exact User Flow (4 clicks max)
1. Click extension icon → automatically opens new tab:  
   `chrome-extension://<id>/app.html`

2. In that tab you see a clean, beautiful UI (see mockup below)

3. Write note → pick image → choose date/time → click “Schedule”  
   → note saved locally + appears in the list

4. Close the tab, forget about it  
   → extension still posts automatically at the right time (Chrome running)

### UI in the new tab (app.html) – Clean & Familiar

```
┌──────────────────────────────────────────────────────────────┐
│  Substack Notes Scheduler                               [← Back] │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  What’s on your mind?                                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Start typing…                                         │ │
│  │  Bold, italic, links, etc. (basic rich text or Markdown) │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  📷 Drop image here or click to upload                       │
│  [Preview of uploaded image]                                 │
│                                                              │
│  Schedule for:  [ Tomorrow at 9:00 AM ▼ ]                     │
│                 (nice date/time picker, timezone aware)     │
│                                                              │
│                       [  Schedule Note  ]                    │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  Upcoming Notes (12)                                         │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Dec 10 – 08:00  GM world                               │ │
│  │ Dec 11 – 14:30  New essay teaser + image               │ │
│  │ Dec 13 – 19:00  Weekend thread                         │ │
│  └────────────────────────────────────────────────────────┘ │
│  [Clear all]     [Export queue]     [Import queue]           │
└──────────────────────────────────────────────────────────────┘
```

### File structure (still only 6 tiny files)

```
substack-notes-scheduler/
├── manifest.json
├── app.html          ← the full-page UI you asked for
├── app.js
├── background.js     ← alarms + auto-post logic
├── content.js        ← runs on substack.com to actually click “Post”
└── icons/
```

### How the auto-post still works
- `background.js` runs every minute (`chrome.alarms`)
- `chrome.alarms.create("check", {delayInMinutes: 1, periodInMinutes: 1})`)
- When a note is due → opens hidden tab to `https://substack.com/notes/new`
- `content.js` fills the textarea, uploads image if any, clicks the real “Post” button
- Closes the tab → done

### Ready in < 250 lines of code, zero dependencies

Want me to drop the complete working code right now (copy → load as unpacked extension → works instantly)?  
Just say “yes” and I’ll paste all 6 files.